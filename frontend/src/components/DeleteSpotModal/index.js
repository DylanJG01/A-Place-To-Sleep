import { useDispatch } from 'react-redux'

const DeleteSpotModal = (spot, user) => {


    return (
        <button className="delete-spot-button">Delete Spot</button>
    )
}

export default DeleteSpotModal